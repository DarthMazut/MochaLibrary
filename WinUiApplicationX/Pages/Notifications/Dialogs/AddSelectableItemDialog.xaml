<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="WinUiApplicationX.Pages.Notifications.Dialogs.AddSelectableItemDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiApplicationX.Pages.Notifications.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    Title="Add selectable item"
    PrimaryButtonText="Add item"
    PrimaryButtonCommand="{Binding AddItemCommand}"
    IsPrimaryButtonEnabled="{Binding CanCreate}"
    CloseButtonText="Cancel">

    <StackPanel Spacing="20">
        <TextBox Header="Key" Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Header="Name" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</ContentDialog>
