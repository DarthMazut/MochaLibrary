<Page
    x:Class="NavigationTest.Pages.Page1.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NavigationTest.Pages.Page1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Resources>
            <Style x:Key="TextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="Foreground" Value="Pink" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Command="{Binding NavigateForwardCommand}" VerticalAlignment="Center" Margin="10">
            <FontIcon Glyph="&#xE72A;" />
        </Button>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <TextBlock Text="{Binding PageName}" Style="{StaticResource TextStyle}" />
            <TextBox Text="{Binding InputText, Mode=TwoWay}"/>
            <Button Content="Show notification" Click="em_ShowNotification" />
        </StackPanel>
    </Grid>
</Page>
