<Window
    x:Class="WinUIApplication.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApplication"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:i="using:Microsoft.Xaml.Interactivity" 
    xmlns:c="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Grid x:Name="xe_rootGrid">
        <i:Interaction.Behaviors>
            <c:EventTriggerBehavior EventName="Loaded">
                <c:InvokeCommandAction Command="{Binding OnLoadedCommand}"/>
            </c:EventTriggerBehavior>
        </i:Interaction.Behaviors>
        <TextBlock Text="Only a sith lord deals in absolutes." HorizontalAlignment="Center" FontSize="24" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Command="{Binding OpenDialogCommand}">Click to open dialog ;)</Button>
        </StackPanel>
    </Grid>
</Window>
