<Page
    x:Class="WinUiApplication.Pages.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiApplication.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Text="Hello There!" />
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Navigate" Command="{Binding NavigateCommand}" HorizontalAlignment="Center" />
            <Button Content="Open Dialog" Command="{Binding OpenDialogCommand}" HorizontalAlignment="Center" />
        </StackPanel>
        <ProgressRing FontSize="40" IsActive="{Binding IsActive}"/>
    </Grid>
</Page>
