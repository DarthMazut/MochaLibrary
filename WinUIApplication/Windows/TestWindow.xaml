<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUiApplication.Windows.TestWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiApplication.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Text="{Binding Text}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button HorizontalAlignment="Center" Command="{Binding CloseCommand}">
                Close
            </Button>
            <Button HorizontalAlignment="Center" Command="{Binding MaximizeCommand}">
                Maximize
            </Button>
        </StackPanel>

    </Grid>
</Window>
